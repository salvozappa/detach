${DETACH_DOMAIN} {
    # WebSocket proxy to bridge (must be handled first)
    handle /ws {
        reverse_proxy bridge:8081
    }

    # Static files with SPA routing
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
